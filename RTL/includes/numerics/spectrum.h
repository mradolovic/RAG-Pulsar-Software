#ifndef __SPECTRUM__
#define __SPECTRUM__
#include <math.h>
//Compressed spectrum calculator
void spectrum(double * restrict sum, int PTS, double * restrict pdat, double * restrict ftdat, double * restrict ftdat2);
#endif
